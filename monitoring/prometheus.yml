global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  - job_name: 'node_app'
    static_configs:
      # Docker container talking to host on Linux is usually 172.17.0.1
      - targets: ['172.17.0.1:3000'] 
